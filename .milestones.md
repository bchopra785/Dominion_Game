# Group Milestone 1

## ATG v1.0

In this milestone, your team will rewrite or refactor your Automation: The Game prototype to add more functionality, and use the interfaces in the provided ATG API. You will also implement two different game players: a human player which takes input from the console, and an automated player that follows a well-defined purchasing strategy. See "Resources" below for links to the API.

The five tasks for this milestone are:
1. Game engine implementation
2. Game harness implementation
3. Console player implementation
4. "Big Money" automated player implementation
5. Code review

## Timeline

This milestone is expected to be completed within roughly three one-week "sprints". For each sprint, you will plan work, submit your progress, and retrospect in order to identify ways to improve in the future.

## Submission

These artifacts should be submitted at the end of each sprint.
1. (Team facilitator) Submit the sprint plan PDF on Moodle
2. (Team facilitator) Create a GitHub release of your repository and submit the zip file from your release on Moodle.
	 1. Create a new tag for each release, named sprint<#>, replacing <#> with the sprint number.
3. (Team facilitator) Submit the end of sprint report PDF on Moodle
4. (All team members) Fill out the peer feedback form

When the milestone is complete, the team facilitator should create a GitHub release of your repository. Create a new tag for the release, named milestone1. The zip file of the release should be submitted on Moodle. This additional submission step should be completed even if the code is the same as an end-of-sprint submission.

Updated 12/4/25
(Update for Spring '25)

## Grading

Grading for sprints uses the standard sprint grading criteria.

Grading for the milestone will focus on how well your engine implements the rules of the game, how usable the console player and harness are, whether your engine, players, and harness are bug-free, and how well your team responded to code review feedback.

## General Advice

Plan your sprints wisely. Leave plenty of time for merging, code reviews, testing, and bug fixing. If you plan to fully complete all 4 stories right before the end of the 3rd sprint, you will have no time to test or get feedback, and will likely need to use a 4th sprint to finish the milestone, which in turn will crunch the schedule for later milestones. You will also likely receive lower individual sprint scores due to lack of work being merged to main each sprint.

Think about how to parallelize work. For example, if you plan to fully complete one major story per week, without working on the others at all, you will not have enough work to keep everyone busy each week, and your last feature will end up squeezed for time when your first features take longer than expected.

Do not over-design up front. When you first start to code on a larger project, it is easy to get paralyzed by the need to fully design the system before starting. Do not let this paralyzation happen - some initial design is useful, but when you start to go around in circles, the best thing to do is to start coding to learn more about the problem.

Do not under-design up front. Conversely, if your team does not discuss an initial approach at all, multiple people will end up duplicating work or working in incompatible ways. Talk about, and try to agree on some high level components, without slipping into over-designing.

Communicate often. Your initial plans, ideas, and designs will change as you get into the project. Communicate roadblocks, changes, or new ideas to your team as soon as possible so that the team can work in a coordinated fashion.

Test and refactor early and often. Practice Test Driven Development, or, failing this, write a small amount of code and test it before moving on. When your code is not easily testable, refactor it to modularize. Do not be afraid to introduce helper classes or functions. The more you encapsulate common or complicated functionality in well tested classes or functions, the easier the later projects in this class will be.

Work in small increments. You are used to completing entire projects at once, submitting them, and moving on to the next project. This is a bad habit that you should break - identify small changes or additions, complete them (and test them), then commit and merge those changes to main, or at least to a branch. Working on huge changes for a long time, and then merging them all at once is disruptive to the rest of your team, and will cause hard-to-find bugs that slow you all down.

Use code generation tools productively. The amount of work in this milestone is designed with the expectation that you will use code generation tools to assist with the implementation and testing. However, the body of work here is likely too large and complex to wholesale delegate to a coding agent. Additionally, part of the required challenge of the assignment is figuring out how to split up and parallelize work across multiple team members. Experiment with code generation tools to find the most effective and efficient ways to leverage them, but do not expect to be successful if you attempt to completely outsource the project to them.

## Resources

- ATG API
	- You want version 1.5.x (the most recent 1.5 version, e.g. 1.5.2 as of this writing)
		- Do not use the 2.x versions!
	- Package on Maven Central
	- Javadocs
- You are free to use any language features or Java standard libraries for all assignments.
- You are free to use any third party libraries available via the Maven Central Repository.
- You are free to use code generation tools (Copilot, ChatGPT, Gemini, etc) for all assignments.
- You can play the full Dominion game free online here: https://dominion.games/ (registration required)

Updated 12/4/25
(Update for Spring '25)

## Story 1: Game Engine

In this story, you will implement an Engine.

### Story 1 deliverables

1. Classes that implement the Engine interface, and support all cards in the 1.5.x API
2. Unit test coverage for the Engine class and any other utility classes created to support the Engine.

Updated 12/4/25
(Update for Spring '25)

## Story 2: Console Player

In this story, you will implement a Player that is controlled by a human using the console. Note that usability is graded. Think about the information that needs to be made available to a human making decisions during the game, and consider ways to streamline their play experience.

### Story 2 deliverables

1. An implementation of Player which prompts for input on the console when necessary
2. Unit test coverage of the console player class

Updated 12/4/25
(Update for Spring '25)

## Story 3: Big Money Player

In this story, you will implement a Player that follows a well-defined strategy. Choose from one of the strategies below (basic, advanced 1, advanced 2), or a combination of the three.

### "Big Money" Player Basic Strategy

A "big money" player will always buy a Framework card if possible; otherwise, it will buy the highest valued money card that it can afford.

### (Optional) Advanced Strategy 1: Boost Hand Expected Value

A "big money" player can follow a more sophisticated strategy: Frameworks are only purchased if the current hand can afford it, AND if the expected money value of a hand from the player's deck is close to or greater than the cost of a Framework.

The end result of following this strategy is that more money will be purchased initially, which will allow the player's deck to maintain purchasing power even after it is diluted by Framework purchases.

### (Optional) Advanced Strategy 2: Do not end the game if losing

A "big money" player can keep track of the number of victory points purchased by itself and its opponent(s), and avoid purchasing the last Framework (and instead purchase a Module or Method) if they are not winning.

### Story 3 deliverables

1. An implementation of Player which follows at least the "basic" strategy above
	 1. The advanced strategies are completely optional.
2. Unit test coverage of the big money player class

Updated 12/4/25
(Update for Spring '25)

## Story 4: Game Harness

In this story, you will implement a console-based harness that creates some players and executes a game.

### Story 4 deliverables

1. A "main" class which executes a game of Automation: The Game
2. The exact form factor of the harness is up to you, but it must:
	 1. Get user input from the console when needed
	 2. Print game events to the console
	 3. Report the results of the game to the console
	 4. Support running a game with all automated players, all human players, or a mix
3. At least one test of your harness

### Story 4 Advice

For the integration test, keep in mind that your Engine is unit tested already. The harness test does not need to validate gameplay - it needs to validate that the harness correctly sets up and uses Players and Engines, and correctly reports game events and results. Think about the testing strategies we have discussed in class - how can you make this type of testing simpler than testing your entire codebase?

Updated 12/4/25
(Update for Spring '25)

## Code review

During each major milestone, your group will need to schedule a code review with either the instructor or your team's assigned TA. The output of the code review is a combination of required and optional changes. Your team must implement the required changes in order to fully complete the milestone.

### Timing of the code review

The best time for a code review is during the second sprint of a milestone, or early in the third sprint. This allows time for some changes from the milestone to be landed on the main branch, but also leaves enough time afterward to implement the required changes.

### Preparation for the code review

Create a branch named code-review-milestone-<#> (replacing <#> with the number of the milestone) from the git tag that marked the release from your previous milestone.

Open a pull request from the tip of your main branch into this new branch. The description of the pull request should include a high level summary of the changes to be reviewed, and any other information that will help the reviewer navigate the changes.

Assign the course staff member who will be reviewing your code and the instructor as the "Reviewers" on the pull request. The code reviewer will leave their feedback on this pull request.

### Code review process

Code reviews are performed synchronously - the reviewer and the team should be together (in person if at all possible, virtually if not) for the review. The entire team is strongly encouraged to attend; however, if schedule conflicts preclude this, a time should be chosen when at least 2 team members can attend.

The reviewer will "drive" (navigate the code on their laptop). The code review is intended to be collaborative and interactive - discussion between the reviewer and team is encouraged. The code reviewer will leave comments on the pull request during the review to summarize discussions and document suggestions.

Required changes will be explicitly labeled as such - any comment on a code review that is not clearly specified as required is optional. The code reviewer may take a short amount of time after the review to mark the set of required changes, and will notify the team when the feedback is ready to be acted upon.

Updated 12/4/25
(Update for Spring '25)

# Group Milestone 2

## ATG v2.0

In this milestone, your team will extend the first version of Automation: The Game to include more action cards, introduce a "network player", and implement a new automated player.

The tasks for this milestone are:
1. Updates to support API v2
2. "Network Player" implementation
3. Implement another automated player
4. Code review

## Timeline

This milestone is expected to be completed within roughly four one-week "sprints". For each sprint, you will plan work, submit your progress, and retrospect in order to identify ways to improve in the future.

## Submission (each sprint)

These artifacts should be submitted at the end of each sprint.
1. (Team facilitator) Submit the sprint plan PDF on Moodle
2. (Team facilitator) Create a GitHub release of your repository and submit the zip file from your release on Moodle.
	 1. Create a new tag for each release, named sprint<#>, replacing <#> with the sprint number. This milestone covers sprints 5-7.
3. (Team facilitator) Submit the end of sprint report PDF on Moodle
4. (All team members) Fill out the peer feedback form

When the milestone is complete, the team facilitator should create a GitHub release of your repository. Create a new tag for the release, named milestone2. The zip file of the release, along with the JAR from story 3, should be submitted on Moodle. This additional submission step should be completed even if the code is the same as an end-of-sprint submission.

## Grading

Grading for sprints uses the standard sprint grading criteria.

Grading for the milestone will focus on how well your engine implements the v2 API, your unit test coverage of action cards, the implementation of your network player, how sophisticated your new automated player is, and how well your team responded to code review feedback.

## General Advice

The general advice from Milestone 1 still applies here. In addition:

Carefully sequence your work. This is a coding-heavy milestone - the API updates, new action cards, network player, and new automated player are each potentially substantial projects on their own. Think carefully about how to split up and sequence your work - this will be critical in order to make effective use of your time, avoid unnecessary merge conflicts, and finish the milestone on schedule.

Consider refactoring your Engine. In an unfactored codebase, an AI coding agent will happily generate overly complex, buggy code to support the new action cards, then go in circles trying to test and debug it. If you can factor your action card handling into a set of methods that mutate a state object and do not depend directly on an engine or full game setup, they will be much more independent, testable, and AI-modifiable than a less modular design. The Command design pattern is also a good fit for this problem. Regardless of the specific design, if the action cards follow good class design principles (cohesion, separation of concerns, loose coupling), then adding support for the new cards will be a task that an AI agent can complete more reliably.

Any serious refactoring should be a standalone change, with no behavior change. If you decide to undertake a refactoring, do it before trying to change behavior. Do not refactor and change functionality at the same time - the refactor should be committed, pushed, and merged as a standalone change. Additionally, you should prioritize landing any big refactoring on the main branch early in the milestone - leaving it until later increases the chance and severity of nasty merge conflicts.

Do not underestimate the amount of time needed to adopt new technologies. For this milestone, you will be writing a HTTP webserver and using Spring - likely for the first time. Things will take longer than you expect, even if you rely on an LLM to generate and test the code. Plan with this in mind!

## Story 1: API v2 Support

In this story, you will update your engine and harness to use v2.5.3 of the atg-api, which includes more action cards, as well as Observers and Events.

See the Card.Type documentation for the complete list of cards that must be supported. Be sure to read the detailed descriptions of each card (in the "Enum Constant Details" section) for implementation details.

Think carefully about how to unit test the action card logic - we will be specifically looking for test coverage that targets the Engine's handling of each action card.

If javadoc.io is down, use this backup site for the javadocs.

### Story 1 deliverables

1. Engine, harness, and player code updated to use v2 of the API
2. Unit test coverage for each Action card

## Story 2: Network Player

In this story, you will create the client and server for a network-based player.

The "player client" will implement the Player interface from the ATG API, and be used directly by the Engine. It will not contain any decision-making logic, and instead will request decisions from a remote HTTP server - the "player server". The player server will contain all the necessary decision-making logic, and respond with the chosen decision. The player client itself should not contain any player logic, and should be able to work with any player server which follows the server API specifications (found below).

The "player server" will be an HTTP server. It will use an "RPC" (remote procedure call) API style. It will accept POST requests with JSON bodies that represent the game state and possible decisions, and respond with a chosen decision.

### Specifications

#### HTTP Server

The player server should expose two HTTP endpoints:

- /decide (POST)
	- Expects a JSON body formatted according to the "DecisionRequest" schema below.
	- Successful (200) responses should include a JSON body formatted according to the "DecisionResponse" schema below.
	- Unsuccessful responses should use appropriate 4xx or 5xx HTTP response codes.
- /log-event (POST)
	- Expects a JSON body formatted according to the "LogEventRequest" schema below.
	- Successful (200) responses should have empty bodies.
	- Unsuccessful responses should use appropriate 4xx or 5xx HTTP response codes.

#### Request / Response formats

The formats below are specified using the JSON Schema format. (Be sure to read up on the basics of JSON Schema so you can understand the expected structure!)

DecisionRequest

```json
{
	"title": "DecisionRequest",
	"description": "An object representing a request for a player to choose a decision",
	"type": "object",
	"properties": {
		"state": {
			"description": "A serialized GameState object",
			"type": "GameState"
		},
		"options": {
			"description": "The list of possible Decisions",
			"type": "array",
			"items": {
				"description": "A serialized Decision object",
				"type": "Decision"
			}
		},
		"reason": {
			"description": "A serialized Event object",
			"type": "Event"
		},
		"player_uuid": {
			"description": "A unique identifier for the player making the request",
			"type": "string"
		}
	}
}
```

DecisionResponse

```json
{
	"title": "DecisionResponse",
	"description": "An object representing a decision chosen by the player",
	"type": "object",
	"properties": {
		"decision": {
			"description": "A serialized Decision object",
			"type": "Decision"
		}
	}
}
```

LogEventRequest

```json
{
	"title": "LogEventRequest",
	"description": "An object representing a game event that can be logged",
	"type": "object",
	"properties": {
		"state": {
			"description": "A serialized GameState object",
			"type": "GameState"
		},
		"event": {
			"description": "A serialized Event object",
			"type": "Event"
		},
		"player_uuid": {
			"description": "A unique identifier for the player this event is being sent to",
			"type": "string"
		}
	}
}
```

In the schema above, several objects have "types" that correspond with types provided by the ATG API. These types are already configured to serialize themselves to JSON in the appropriate schemas, and so the details of those schemas are not included here.

### Resources & Recommendations

#### Server language

Your HTTP server can be written in any language - this is one of the benefits of modularization at the system level. However, it is highly recommended to implement it in Java, so you can use your existing player implementations to make decisions, rather than needing to implement a new player from scratch. In Java, you also benefit from using all of the classes provided by the ATG API. But, if you would prefer to try another language, you are free to do so.

#### Java Server Framework

There are many ways to build an HTTP server in Java. The recommended approach for this project is to use Spring. Spring is a very heavyweight, opinionated Java framework that makes it possible to implement a good, compliant webserver with very little code. The price of this, though, is that the Spring framework itself is large and complex. You only need to learn a tiny slice of it in order to get a webserver working, but identifying which tiny slice is a challenge.

#### Reference Repository

Here is a sample repo which contains a multi-module Maven project, with a Spring sub-module, and an example of installing the Guava module needed to support parsing/serializing Guava objects to JSON: https://github.com/brandeis-cosi-103a/spring-server-example. This is a very close match to what you will need to do to get a Spring server running!

#### Other resources

- How to write a RESTful service with Spring
	- Following this guide will get you an HTTP server that accepts GET requests and responds with JSON.
- JSON parsing and Jackson
	- Jackson is the library which handles JSON serialization and parsing.
	- This is what will be used to turn classes into JSON when making network requests, and to parse JSON back into classes when receiving a network request.
	- If Jackson is on the classpath, Spring will use it automatically.
	- The JSON serialization and parsing will almost seem like magic if you set things up correctly - if you find yourself doing any manual JSON parsing in the server, you are probably not on the right track.
	- Jackson tutorial (a general resource on Jackson)
- Guava
	- The ATG API uses Guava's immutable containers (e.g. ImmutableList), which Jackson cannot handle by default. So, we need to customize the Jackson module by installing a Guava module in our Spring application.
	- Spring Boot: Customizing the Jackson ObjectMapper
	- Guava Spring module
	- StackOverflow relevant thread
	- Source code for JacksonAutoConfiguration
		- You do not need to look deeply at this file, but it is included so you can see that, even though the configuration seems to work "magically", that it is actually implemented through a sophisticated, well-encapsulated class.
- Spring Boot in Visual Studio Code
	- This VSCode extension can make it easier to create a Spring application in your project.

### Story 2 deliverables

1. Code for a player client: An implementation of the Player interface which makes decisions by making a network call to a remote server
2. Code for a player server: An HTTP webserver which can respond to the requests made by the player client

## Story 3: New Automated Player

In this story, you will implement a new automated player that takes advantage of the action cards in the game.

There are many possible strategies to pursue. The minimum requirement for this player is that it is capable of purchasing and using at least 2 different action cards. In this milestone, the performance of your player is not measured.

### Story 3 deliverables

1. A new automated player which uses at least 2 different action cards
2. Unit tests for the new player

### Resources

- Beginner Dominion strategy guide in 6 parts
- Dominion strategies wiki
- Five fundamental Dominion deck types

## Code review

During each major milestone, your group will need to schedule a code review with either the instructor or your team's assigned TA. The output of the code review is a combination of required and optional changes. Your team must implement the required changes in order to fully complete the milestone.

### Timing of the code review

The best time for a code review is during the second sprint of a milestone, or early in the third sprint. This allows time for some changes from the milestone to be landed on the main branch, but also leaves enough time afterward to implement the required changes.

### Preparation for the code review

Create a branch named code-review-milestone-<#> (replacing <#> with the number of the milestone) from the git tag that marked the release from your previous milestone.

Open a pull request from the tip of your main branch into this new branch. The description of the pull request should include a high level summary of the changes to be reviewed, and any other information that will help the reviewer navigate the changes.

Assign the course staff member who will be reviewing your code and the instructor as the "Reviewers" on the pull request. The code reviewer will leave their feedback on this pull request.

### Code review process

Code reviews are performed synchronously - the reviewer and the team should be together (in person if at all possible, virtually if not) for the review. The entire team is strongly encouraged to attend; however, if schedule conflicts preclude this, a time should be chosen when at least 2 team members can attend.

The reviewer will "drive" (navigate the code on their laptop). The code review is intended to be collaborative and interactive - discussion between the reviewer and team is encouraged. The code reviewer will leave comments on the pull request during the review to summarize discussions and document suggestions.

Required changes will be explicitly labeled as such - any comment on a code review that is not clearly specified as required is optional. The code reviewer may take a short amount of time after the review to mark the set of required changes, and will notify the team when the feedback is ready to be acted upon.
